# コピーして .env として使ってください(.env は .gitignore 済みです)。
#   cp .env.example .env
# これで school-pipeline を実行するたびに ANTHROPIC_API_KEY を export する必要がなくなります。
# (Claude Code自体のログインには影響しません。このファイルはschool-pipelineコマンドだけが読みます。)

ANTHROPIC_API_KEY=sk-ant-your-key-here
